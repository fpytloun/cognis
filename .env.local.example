# Local Compose deployment example.
# Copy to .env.local or export these variables before running Make targets:
#   set -a; source .env.local; set +a

# Required for the seeded Cognis provider and recommended Mnemory/Intaris LLMs.
# The repository does not resolve Cognis credential IDs; callers may populate
# these from credential id "litellm" externally.
COGNIS_LOCAL_LLM_BASE_URL=https://api.groq.com/openai/v1
COGNIS_LOCAL_LLM_API_KEY=

# Deterministic single-user local admin. Change for any non-disposable setup.
COGNIS_LOCAL_ADMIN_EMAIL=admin@localhost
COGNIS_LOCAL_ADMIN_PASSWORD=cognis-local-admin
COGNIS_LOCAL_ADMIN_NAME=Local Admin

# Recommended cheap defaults; any OpenAI-compatible endpoint/model can be used.
COGNIS_LOCAL_CHAT_MODEL=gpt-oss-120b
MNEMORY_LLM_MODEL=gpt-oss-120b
MNEMORY_FIND_LLM_MODEL=gpt-oss-120b
MNEMORY_FSCK_LLM_MODEL=gpt-oss-120b
MNEMORY_CONSOLIDATION_LLM_MODEL=gpt-oss-120b
MNEMORY_EMBED_MODEL=text-embedding-3-small
MNEMORY_EMBED_DIMS=1536
INTARIS_LLM_MODEL=gpt-oss-20b
INTARIS_ANALYSIS_LLM_MODEL=gpt-oss-120b
INTARIS_ANALYSIS_L3_LLM_MODEL=gpt-oss-120b
INTARIS_JUDGE_LLM_MODEL=gpt-oss-120b
INTARIS_SEARCH_EMBEDDING_MODEL=text-embedding-3-small
INTARIS_SEARCH_EMBEDDING_DIM=1536
INTARIS_SEARCH_SPARSE_MODEL=Qdrant/bm25

# Published dependency images. Pin these in reproducible environments.
MNEMORY_IMAGE=genunix/mnemory:latest
INTARIS_IMAGE=genunix/intaris:latest

# Cognis builds from this repository by default. Set these only when using
# prebuilt images through a compose override workflow.
COGNIS_IMAGE=cognis-local:latest
COGNIS_EXECUTOR_IMAGE=cognis-executor-local:latest

# Host ports.
COGNIS_PORT=8080
MNEMORY_PORT=8050
MNEMORY_MGMT_PORT=9090
INTARIS_PORT=8060
QDRANT_PORT=6333
